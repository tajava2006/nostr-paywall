{
  "name": "nostr-paywall",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "verify-publish": "node scripts/verify-publish.mjs"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^5.0.0"
  }
}
