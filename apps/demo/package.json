{
  "name": "@nostr-paywall/demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "이벤트 단위 유료 릴레이 데모 — 아웃박스 모델 준수 클라이언트",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "deploy": "pnpm build && nsyte deploy ./dist"
  },
  "dependencies": {
    "@nostr-paywall/client": "workspace:*",
    "@nostr-paywall/float": "workspace:*",
    "@nostr-paywall/protocol": "workspace:*",
    "nostr-tools": "^2.7.2"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.0",
    "ws": "^8.21.3"
  }
}
