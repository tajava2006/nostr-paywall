packages:
  - 'packages/*'
  - 'apps/*'

# vitest 가 esbuild 네이티브 바이너리를 postinstall 로 받는다. pnpm 11 은 기본 차단이라
# 이 레포에서만 명시적으로 허용한다.
allowBuilds:
  esbuild: true

minimumReleaseAgeExclude:
  - '@cashu/cashu-ts@4.10.1'
